import { Box } from "@material-ui/core"
import { Typography } from "@mui/material";

const Gallery = () => {
    return (
        <Box>
            <Typography>
                Gallery page
            </Typography>
        </Box>
    )
}
export default Gallery;